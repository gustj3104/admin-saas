services:
  postgres:
    image: postgres:16
    container_name: admin-saas-postgres
    environment:
      POSTGRES_DB: admin_saas
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin1234
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
